## Autopsy: `btc-updown-5m-1783041300` (settled DOWN, −$123.28) — and v5.4 is tick-identical on this bar (0 diffs), so this is a shared failure mode.

### The bar's true story, told by the one input that never lied

**Flow was negative from the opening seconds and never actually agreed with the mid-bar price pop.** `cvd_since_open` opened at −188,907, dipped to a low of −408,350, and closed at −244,638 — negative on every observed tick, including the whole stretch where price rallied. `cvd_d3m` told a two-act story: strongly positive early (+1.35M, still +1.22M mid-bar) before collapsing to −215,626 by the last tick, tracking the shift from the up-pop to the down-crack. `large_prints` was net-negative on every one of 43 nonzero ticks — whale flow never once turned positive, even while price and book both leaned up. The only liar all bar was **cushion**, which spent 29 of 43 ticks positive (peak +$16.50) before crashing to −$103.30 by the final observed tick, and **book ewma**, which also flipped sign with price rather than flow. This is the absorption shape: price popped on a positive cushion while `cvd_since_open` and `large_prints` sat negative underneath it the whole time, and the bar cracked down to settle exactly where the persistent flow said it would. Note: one duplicate-second row in the log; not mid-bar-joined.

### Episode by episode

| rem | call | ticks | why it fired | verdict |
|---|---|---|---|---|
| 295→284 | MIXED | 12 | no entry yet, cushion drifting from open toward +4.71 | — |
| 283→265 | **UP** | 19 | cushion +4.72 (aligned, same side) at the discounted 0.14 book-ewma bar; ewma +0.41 comfortably cleared it; d3m flow agreed (+1.23M) but `cvd_since_open` was already −288,197 | **WRONG** |
| 264→211 | MIXED | 10 | cushion grew to +16.49 (ratio 0.79, still sub-floor) then crashed to −66.59 intra-episode while book ewma went dead (0.009) | — |
| 185→117 | **DOWN** | 3 | cushion −123.28 (ratio 6.2× floor, fat), ewma −0.189 agreeing, momentum confirmer fired DOWN, whale prints net negative across the episode; d3m was still +472,405 (lagging) but `cvd_since_open` was −408,350, the most extreme of the bar | RIGHT |

### Why it was right when it was right

The DOWN call converged on everything that matters for a fat-cushion entry: cushion was 6.2× the vol floor (deep into the ≥2×-floor "fat" zone patterns.md measures at 94–98%), book ewma had flipped to agree, and momentum actually fired DOWN as a confirmer. The one dissenter was `cvd_d3m`, still nominally positive at entry — but it was outvoted by the more extreme `cvd_since_open` read (−408,350, the bar's low) and by whale prints, which never printed positive all bar. This wasn't a coin-flip hold; it was a fat cushion with three of four voices agreeing.

### Why it was wrong when it was wrong — the pattern, precisely

The one wrong episode has no flag attached in the script output, so it is described plainly rather than mapped to a named class. The UP entry at rem 283 fired on a genuinely thin cushion (ratio 0.14× floor — an order of magnitude below the fat threshold) using the discounted 0.14 aligned-entry bar, and book ewma (+0.41) and `cvd_d3m` (+1.23M) both agreed with the call at that moment. What the entry didn't see: `cvd_since_open`, the full-bar cumulative flow, was already −288,197 — deeply negative even as the 3-minute window and the book leaned up. The engine has no mechanism to weigh the longer flow window against the shorter one when they disagree; it entered on the corroborators that happened to be pointed the right way at that instant. This resembles the absorption shape from `1783188300` (flow right, book+cushion wrong) but the mechanics differ enough from that autopsy's `thin-aligned-vs-flow` case that the script did not flag it here — chiefly because `cvd_d3m` was *not* opposite the call at entry, only `cvd_since_open` was. BAFO did not have a lever on this episode either way: BAFO only acts on cushions ≥ max($30, 3× vol_1m), and this cushion (ratio 0.14× floor) was nowhere near that fat threshold, so v5.3 and v5.4 replay identically here (0 diffs).

**Candidate rules this autopsy supports** (queue, discussion-first, measured on the full set before touching anything): none new — this bar's miss doesn't match a flagged pattern (no `failure_flags` returned), and the disagreement was in `cvd_since_open` alone rather than the strongly-opposite 3-minute flow the `thin-aligned-vs-flow` candidate targets; log it as unclassified.
